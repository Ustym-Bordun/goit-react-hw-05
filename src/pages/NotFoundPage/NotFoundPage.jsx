import css from './NotFoundPage.module.css';

const NotFoundPage = () => {
  return (
    <div className={css.wrapper}>
      <p className={css.title}>NotFoundPage</p>
      <p className={css.text}>Lorem ipsum dolor sit amet.</p>
    </div>
  );
};

export default NotFoundPage;
